{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && uvicorn enhanced_api:app --host 0.0.0.0 --port 5000",
    "healthcheckPath": "/",
    "workingDirectory": "backend"
  }
}